# Brainstorming 第三轮：实现前最后的深度追问

> 前两轮已覆盖：项目定位、技术选型、核心机制、HITL 状态机、反馈闭环、LLM 适配、记忆/上下文、错误处理、配置文件、机制演示。
>
> 第三轮聚焦于**尚未触及的工程实践问题**——这些问题若不在编码前想清楚，会在实现中途造成返工。

---

## 一、Agent 的"人格"与系统提示

### Q3.1 系统提示设计

Agent 的 system prompt 是决定其行为质量的关键。LLM 在这个 prompt 下被要求扮演什么角色？请给出一个系统提示的草稿框架，至少包含：

- 角色定义（"你是一个……"）
- 可用工具列表及使用时机
- 行为约束（遇到不确定时怎么办？何时该问用户？何时该自主决策？）
- 反馈处理规则（收到测试失败/护栏拒绝后如何调整策略）
- 禁止事项（不能做什么）

### Q3.2 多轮任务中的"目标漂移"防护

LLM 在执行多轮任务时容易"忘记最初的目标"——比如用户说"给 UserService 加单元测试"，第 3 轮 Agent 开始重构整个项目结构。如何防止？

- 是否在每轮上下文开头重复注入原始任务描述？
- 是否有"偏离检测"机制（如对比当前动作与 PLAN 中 task 的预期方向）？
- 偏离检测由谁来做？代码逻辑还是 LLM 自检？

---

## 二、实现策略与开发流程

### Q3.3 PLAN.md 的执行策略

PLAN.md 中 31 个 task 分为 T0-T10 共 11 个阶段。你的实际执行策略是什么？

| 问题 | 你的决策 |
|------|---------|
| 哪些 task 可以在独立 worktree 中并行开发？ | |
| 哪些 task 必须严格串行（依赖链）？ | |
| 每个 task 由哪个 subagent 完成？你打算用同一个 subagent 还是不同模型？ | |
| T0 脚手架搭建后，是否需要先写一个"冒烟测试"（最简主循环跑通 mock LLM）再进入功能开发？ | |
| 核心机制（护栏、反馈、主循环）的开发顺序：先全部实现再集成，还是逐个实现并集成？ | |

### Q3.4 多语言项目支持

SPEC 和 PLAN 默认假设目标项目是 TypeScript/Node.js，但 `codeharness` 本身应该能用于任何语言的项目。以下场景如何处理？

| 场景 | 处理方式 |
|------|---------|
| 用户在 Python 项目中运行 `codeharness`（没有 `package.json`） | |
| 测试命令是 `pytest`、lint 是 `flake8`、类型检查是 `mypy` | |
| 项目没有 `.git` 目录（非 Git 项目） | |
| 项目是 monorepo（多个子项目，各自有独立的测试/lint 命令） | |
| 用户的项目使用 Makefile / Taskfile 作为统一入口 | |

### Q3.5 开发环境与工具链

| 问题 | 你的决策 |
|------|---------|
| 包管理器：npm / yarn / pnpm？ | |
| TypeScript 编译目标：ES2022？Node 最低版本要求？ | |
| 代码风格：Prettier？ESLint？哪种规则集？ | |
| 是否使用 monorepo 工具（turborepo / nx）？还是简单的单包结构？ | |
| Git 分支策略：直接在 main 开发？还是 feature 分支 + PR？ |

---

## 三、运行时行为

### Q3.6 流式输出与用户体验

LLM 调用通常需要数秒到数十秒。在此期间用户看到什么？

| 问题 | 你的决策 |
|------|---------|
| 是否流式输出 LLM 的思考过程？还是只显示"正在思考..."的 spinner？ | |
| 是否在每轮执行前展示 Agent 的"计划"（"我打算：1. 读取 x.ts 2. 修改 y 函数 3. 运行测试"），让用户预览并确认？ | |
| 执行命令时是否实时展示 stdout/stderr？还是只展示最终结果？ | |
| 是否支持 verbose 模式（展示完整 LLM 请求/响应）用于调试？ | |

### Q3.7 Dry-Run 模式

是否支持"仅规划不执行"的模式？

```
$ codeharness --dry-run "重构 UserService"
# Agent 分析代码库 → 输出计划:
#   1. 读取 src/user.ts
#   2. 提取 UserService 类到独立文件
#   3. 更新所有 import
#   4. 运行测试验证
# 确认执行？[Y/n]
```

如果支持，dry-run 本身是否消耗 LLM 调用？Agent 在 dry-run 中是否允许读取文件（只读操作）？

### Q3.8 任务完成的判定

Agent 何时认为"任务完成"？什么情况下 Agent 可能"错误地宣告完成"？

- 如果 Agent 调用了 `finish { success: true }`，但测试仍未通过，harness 是否拦截这个 finish？
- 如果 Agent 调用了 `finish { success: false }`，用户看到什么？是否自动输出失败原因和建议？
- 用户是否可以在 Agent 运行中途说"停，这已经够好了"并强制结束？

---

## 四、LLM 调用策略

### Q3.9 速率限制与重试

LLM API 有速率限制（RPM/TPM）。当遇到 429 错误时：

| 问题 | 你的决策 |
|------|---------|
| 是否自动重试？重试几次？间隔多久（固定/指数退避）？ | |
| 重试时是否向用户展示进度（"API 限流，第 2 次重试..."）？ | |
| 是否在调用前做本地速率限制（令牌桶）以避免触发远端限流？ | |
| 如果 3 次重试均失败，Agent 进入什么状态？是否保存当前进度？ | |

### Q3.10 Token 成本追踪

用户直接为 LLM API 付费，是否应该让用户知道每次任务的成本？

| 问题 | 你的决策 |
|------|---------|
| 是否在任务完成后展示 token 用量和估算成本？ | |
| 是否支持"预算上限"（"这次任务最多花 $0.50"）？ | |
| 是否在审计日志中记录每次 LLM 调用的 token 数和成本？ | |
| 不同供应商、不同模型的定价不同，如何维护价格表？ | |

### Q3.11 模型降级策略

如果用户配置了多个 LLM 供应商，是否支持自动降级？

```
例如：
  主模型: gpt-4o（OpenAI）
  备选: claude-sonnet（Anthropic）
  备选: ollama/qwen（本地）

如果 OpenAI 不可用 → 自动切换到 Anthropic → 如果也不可用 → 切换到本地模型
```

- 是否支持？如果支持，降级时是否通知用户？
- 不同模型能力不同（如上下文窗口大小），降级后行为是否调整？

---

## 五、测试策略

### Q3.12 Harness 自身的测试策略

除了 mock LLM 的单元测试，harness 自身还需要哪些测试？

| 测试类型 | 是否纳入？ | 测什么？ |
|----------|-----------|---------|
| 单元测试（mock LLM） | ✅ 已定 | 护栏、反馈收集器、动作解析器、上下文构建器 |
| 集成测试 | ？ | 完整的"用户输入 → Agent 执行 → 产出结果"流程（仍用 mock LLM） |
| CLI 端到端测试 | ？ | 实际执行 `codeharness "任务"` 命令，验证退出码和输出 |
| 跨平台测试 | ？ | Windows/Linux/macOS 上的 CI 矩阵 |
| 性能测试 | ？ | 护栏检测延迟、上下文构建时间 |
| 安全测试 | ？ | 模糊测试危险模式检测器、路径越界检测 |

### Q3.13 测试覆盖率目标

| 问题 | 你的决策 |
|------|---------|
| 整体覆盖率目标？SPEC 中提到 ≥ 80%（核心模块），具体哪些模块算"核心"？ | |
| 是否有文件可以豁免覆盖率（如 CLI 入口、类型定义文件）？ | |
| 覆盖率检查是否加入 CI（不达标则 CI 失败）？ | |

---

## 六、可观测性

### Q3.14 日志与调试

除了审计日志（L5），还需要哪些日志？

| 日志类型 | 内容 | 存储位置 | 日志级别 |
|----------|------|---------|---------|
| 审计日志 | 已定：所有动作 + 护栏结果 + 审批结果 | `~/.codeharness/logs/audit/` | 始终记录 |
| 运行日志 | ？ | ？ | ？ |
| 调试日志 | ？ | ？ | ？ |
| LLM 调用日志 | ？ | ？ | ？ |

- 日志格式：纯文本？JSON 行？结构化日志？
- 日志轮转策略：按大小？按时间？保留多久？
- 是否支持用户自定义日志级别（`--log-level debug`）？

### Q3.15 进度指示

对于长时间运行的任务（如 Agent 执行了 10 轮），用户在终端看到什么？

- 是否显示进度条（"第 3/10 轮"）？
- 是否显示每轮的耗时？
- 是否显示当前正在执行的动作类型（"正在运行测试..."）？
- 非交互模式下是否抑制所有输出，只输出最终结果？

---

## 七、分发与安装体验

### Q3.16 首次运行体验（First-Run Experience）

用户首次安装 `codeharness` 后的完整体验流程是什么？

```
$ npm install -g codeharness
$ codeharness "给 UserService 添加单元测试"

# 此时应该发生什么？
```

请设计完整的首次运行向导流程，包括：
- 检测到未配置 API Key → 引导设置
- 选择 LLM 供应商
- 录入 API Key（隐藏输入）
- 验证 Key 有效性
- 是否生成默认配置文件？
- 是否展示快速入门提示？

### Q3.17 版本管理与升级

| 问题 | 你的决策 |
|------|---------|
| 如何通知用户有新版本可用？ | |
| 配置文件格式升级时（如 v1 → v2 schema 变更），如何迁移？ | |
| 是否检查 Node.js 版本兼容性？最低要求是多少？ | |
| 是否支持 `codeharness --version` 和 `codeharness --help`？ | |

---

## 八、边界情况补充

### Q3.18 超大项目的处理

| 场景 | 处理方式 |
|------|---------|
| 工作区有 10 万个文件，`list_dir` 或 `search_file` 输出巨大 | |
| 单个文件 50MB（如生成的 JSON、锁文件），`read_file` 如何处理 | |
| Agent 尝试 `grep` 一个在所有文件中都出现的模式，返回 10 万行结果 | |
| `node_modules` 有 50GB，Agent 是否应该自动排除？ | |

### Q3.19 用户输入的边界情况

| 场景 | 处理方式 |
|------|---------|
| 用户输入为空（`codeharness ""`） | |
| 用户输入为非 ASCII 字符（中文、emoji） | |
| 用户输入包含 shell 特殊字符（`$`、`\`、`` ` ``） | |
| 用户输入极长（5000 字的需求描述） | |
| 用户输入是明显无意义的内容（"asdfghjkl"） | |

### Q3.20 Agent 自主权限的边界

在什么情况下 Agent **不需要**经过用户同意就可以做决定？在什么情况下**必须**询问用户？

| 场景 | 自主执行 | 需询问 |
|------|---------|--------|
| 选择使用哪个 npm 包来实现功能 | | |
| 修改函数签名（影响其他调用方） | | |
| 删除一个看起来"没用"的文件 | | |
| 添加一个新的项目依赖 | | |
| 修改已有的测试用例（而非只加新测试） | | |
| 重构代码结构（移动文件、重命名） | | |
| 在多个可行方案中选择一个 | | |

---

## 九、反思与元问题

### Q3.21 前三轮 Brainstorming 的反思

在你看来，前三轮 brainstorming 覆盖了 SPEC 所需的所有维度吗？还有什么"你觉得应该讨论但还没被问到"的问题？

### Q3.22 对 SPEC 冻结的承诺

经过三轮 brainstorming，SPEC.md 将进入冻结状态。冻结后若发现设计缺陷：

- 什么级别的缺陷允许修改 SPEC？（安全漏洞？功能缺失？设计矛盾？）
- 修改 SPEC 的流程是什么？（更新 SPEC → 更新 PLAN → 重新评估受影响 task？）
- 是否在 SPEC 中记录变更历史？

---

**请逐一回答以上问题。本轮是进入 SPEC 冻结和代码实现前的最后一次设计审查。**